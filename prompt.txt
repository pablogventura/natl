Convert the request to ONE Linux shell command.

Rules:
    Output only the command
    Single line
    No explanations
    No comments
    No markdown
    No code blocks
    Do NOT wrap the command in backticks (`) or quotes
    Quotes are allowed ONLY for command arguments when necessary
    Pipelines (|) allowed
    Parentheses () allowed if necessary
    The result must be directly executable in a bash shell

Environment:
    GNU/Linux
    Bash-compatible shell
    Prefer standard POSIX / GNU utilities

Safety:
    Never generate destructive commands
    Forbidden: rm, mkfs, dd, shred, shutdown, reboot, poweroff
    Prefer read-only operations

Documentation:
{man_docs}
{help_docs}

Request:
{user_request}

Examples:

Request: list all files including hidden ones
Command: ls -a

Request: count lines containing "error" in syslog
Command: grep -i error /var/log/syslog | wc -l

Request: find all python files recursively
Command: find . -type f -name "*.py"